apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble
spec:
  parentRefs:
    - name: internal
      namespace: envoy-gateway-system
  hostnames:
    - hubble.${cluster_subdomain}
  rules:
    - backendRefs:
        - name: hubble-ui
          port: 80
