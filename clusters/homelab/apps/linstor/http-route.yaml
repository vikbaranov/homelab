apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: linstor
spec:
  parentRefs:
    - name: internal
      namespace: envoy-gateway-system
  hostnames:
    - linstor.${cluster_subdomain}
  rules:
    - backendRefs:
        - name: linstor-controller
          port: 3370
